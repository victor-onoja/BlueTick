import 'package:flutter/material.dart';

class Broadcast extends StatelessWidget {
  const Broadcast({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('Broadcast')));
  }
}
